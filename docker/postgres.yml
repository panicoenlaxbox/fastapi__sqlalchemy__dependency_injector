version: "3.1"

services:

  db:
    image: postgres
    volumes:
        - /C/Temp/postgresql/data:/var/lib/postgresql/data
    environment:
        POSTGRES_PASSWORD: postgres
    ports:
        - "5432:5432"